version: "1"
workflows:
  - name: # Unique identifier for your workflow (required)
      sample-workflow
    description: sample workflow
    public: true
    sourceCodeURL: "ifelsepodcast.com"
    steps:
      - echo hello 1
      - echo hello 2
      - echo hello 3
    env:
      - "USER=world"
      - "OPS_ACCESS_TOKEN"
